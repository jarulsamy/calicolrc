import random

thres=0.1

for x in range(0,800,5):
    for y in range(0,300,5):
        if random.random()>=thres:
            createStar(x,y)



numStars=100

xSpan=800
ySpan=300

for i in range(numStars):
    createStar(xSpan*random.random(),ySpan*random.random())